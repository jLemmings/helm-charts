apiVersion: v1
kind: Service
metadata:
  name: {{ include "winevault.fullname" . }}
  labels:
    {{- include "winevault.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "winevault.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
