apiVersion: v1
kind: Service
metadata:
  name: {{ include "divevault.fullname" . }}
  labels:
    {{- include "divevault.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "divevault.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
