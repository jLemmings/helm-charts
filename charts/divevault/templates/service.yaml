apiVersion: v1
kind: Service
metadata:
  name: {{ include "dive-backend.fullname" . }}
  labels:
    {{- include "dive-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "dive-backend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
